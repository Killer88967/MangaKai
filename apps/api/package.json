{
  "name": "@mangakai/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=.env src/index.ts",
    "build": "tsc --noEmit",
    "start": "tsx src/index.ts",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^2",
    "@hono/zod-validator": "^0.9.1",
    "@mangakai/db": "workspace:*",
    "@mangakai/mangadex": "workspace:*",
    "@mangakai/shared": "workspace:*",
    "hono": "^4",
    "tsx": "^4",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^26",
    "typescript": "^7.0"
  }
}
