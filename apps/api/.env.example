# Postgres connection used by @mangakai/db
DATABASE_URL=postgres://mangakai:mangakai@localhost:5432/mangakai

# Shared secret for /admin routes until real auth lands.
# Send as: Authorization: Bearer <token>
ADMIN_TOKEN=dev-admin-token

PORT=8787
